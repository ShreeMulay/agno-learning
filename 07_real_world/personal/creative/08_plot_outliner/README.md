# Example #208: Plot Outliner Agent

Creates structured story outlines with plot points and pacing.

## Usage
```bash
python main.py --structure three_act
python main.py --structure heros_journey
python main.py --structure save_the_cat
```

# E-Discovery Assistant

Analyzes documents for relevance and privilege in litigation contexts.

## Usage

```bash
python main.py
python main.py --document "Email from..." --context "Contract dispute..."
```

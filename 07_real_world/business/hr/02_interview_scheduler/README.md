# Interview Scheduler

Matches interviewer and candidate availability across time zones to schedule interviews.

## Usage

```bash
python main.py
python main.py --candidate "..." --interviewers "..."
```

# Example #083: ETL Pipeline Builder

Generates ETL pipeline code and configurations from source to target specs.

## Usage
```bash
python main.py --source postgres --target snowflake
```

## Features
- Extract query generation
- Transform step design
- Load strategy selection
- Schedule configuration
- Code generation
